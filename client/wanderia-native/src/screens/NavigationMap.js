import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NavigationMap = () => {
    return (
        <View>
            <Text>NavigationMap</Text>
        </View>
    );
};

export default NavigationMap;

const styles = StyleSheet.create({});
